[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "king-triton-kernel"
version = "0.1.0"
description = "RLVR training pipeline for Triton kernel generation — teach an LLM to write correct and faster kernels (14B · single 8xA800 node)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
authors = [{ name = "King.triton-kernel contributors" }]
keywords = ["triton", "kernel", "rlvr", "reinforcement-learning", "kernel-generation", "cuda"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "torch>=2.8",
    "triton>=3.4",
    "numpy",
    "fastapi",
    "uvicorn>=0.24",
    "pydantic>=2",
    "pydantic-settings",
    "redis",
    "aiohttp",
    "psutil",
]

[project.optional-dependencies]
# Training-side RL stack (verl integration); separate extra, keeps the Apache-2.0
# copyright header (see the note in drkernel/kernel/LICENSE).
train = [
    "verl",
    "ray>=2.4",
    "vllm>=0.8",
    "httpx",
    "pandas",
    "pyarrow",
]
# Evaluation tooling (evals/) aggregation scripts
eval = [
    "pandas",
    "pyarrow",
]
dev = [
    "pytest>=7",
    "pytest-cov",
    "ruff",
    "mypy",
    "pre-commit",
]

[project.scripts]
kernelgym-server = "kernelgym.server.api.server:main"
kernelgym-worker = "kernelgym.worker.gpu_worker:main"
kernelgym-worker-monitor = "kernelgym.worker.worker_monitor:main"
kernelgym-single-worker = "kernelgym.worker.single_worker:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["kernelgym*"]
exclude = ["tests*", "evals*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "gpu: tests that execute kernels on a real NVIDIA GPU",
    "redis: integration tests that require a real redis instance",
    "slow: slow tests / tests that require the eval server",
]
addopts = "-m 'not gpu and not redis and not slow'"

[tool.ruff]
line-length = 110
target-version = "py310"
extend-exclude = ["drkernel", "docs", "evals"]

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
ignore_missing_imports = true

[tool.coverage.run]
source = ["kernelgym"]
omit = ["*/__init__.py", "tests/*"]
