# King.triton-kernel runtime deps
# NOTE: torch/triton 版本下限必须与 pyproject.toml [project.dependencies] 保持一致.
#       pyproject 是 pip 安装的权威来源; requirements.txt 是开发/部署的快捷安装.
#       改动任一文件的 torch/triton 版本时请同步另一文件.
torch>=2.8
triton>=3.4
vllm>=0.8.5
verl  # RL training framework (see drkernel/verl_patch)
ray
fastapi
redis
pydantic>=2
pandas
pyarrow
