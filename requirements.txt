# King.triton-kernel runtime dependencies
# NOTE: version floors must stay in sync with pyproject.toml [project.dependencies].
#       pyproject.toml is the authoritative source for pip metadata; this file is
#       a convenience pin for dev/deploy installs. When you change a version floor
#       in one file, update the other too.
torch>=2.8
triton>=3.4
numpy
vllm>=0.8.5
verl  # RL training framework (see drkernel/verl_patch)
ray
fastapi
uvicorn>=0.24
pydantic>=2
pydantic-settings
redis
aiohttp
psutil
pandas
pyarrow
